Changes made to speed up gem installations:
1. Updated `.github/workflows/ci.yml` replacing `ruby/setup-ruby@v1` with `appraisal-rb/setup-ruby-flash@v1` to leverage rv and ore.
2. Modified `Dockerfile` to install `ore-light` globally and run `ore install` instead of `bundle install` for faster image builds.
3. Patched `bin/setup` and `bin/docker-entrypoint-web` scripts to run `ore check || ore install` instead of bundle counterparts.

Local tests are currently skipping due to a Docker hub rate limit when running `task test` from my IP, but logically the scripts behave the same with a faster implementation. Let me know if further review is needed.
