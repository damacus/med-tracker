[package]
name = "medtracker-web"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.4"
leptos = { version = "0.8.14", features = ["ssr"] }
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread"] }
