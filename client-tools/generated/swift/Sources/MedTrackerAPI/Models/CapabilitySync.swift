//
// CapabilitySync.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct CapabilitySync: Sendable, Codable, Hashable {

    public enum NumericIds: String, Sendable, Codable, CaseIterable, CaseIterableDefaultsLast {
        case backwardCompatible = "backward_compatible"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    /** Whether portable identifiers are supported. */
    public var portableIds: Bool
    public var numericIds: NumericIds
    /** Whether mobile snapshots are supported. */
    public var mobileSnapshot: Bool
    /** Whether dry-run imports are supported. */
    public var dryRunImport: Bool
    /** Whether idempotency keys are supported. */
    public var idempotencyKeys: Bool
    /** Whether ETag conflicts are supported. */
    public var etagConflicts: Bool
    /** Whether the change feed is supported. */
    public var changeFeed: Bool
    /** Whether batch mutations are supported. */
    public var batchMutations: Bool
    /** Whether tombstones are supported. */
    public var tombstones: Bool

    public init(portableIds: Bool, numericIds: NumericIds, mobileSnapshot: Bool, dryRunImport: Bool, idempotencyKeys: Bool, etagConflicts: Bool, changeFeed: Bool, batchMutations: Bool, tombstones: Bool) {
        self.portableIds = portableIds
        self.numericIds = numericIds
        self.mobileSnapshot = mobileSnapshot
        self.dryRunImport = dryRunImport
        self.idempotencyKeys = idempotencyKeys
        self.etagConflicts = etagConflicts
        self.changeFeed = changeFeed
        self.batchMutations = batchMutations
        self.tombstones = tombstones
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case portableIds = "portable_ids"
        case numericIds = "numeric_ids"
        case mobileSnapshot = "mobile_snapshot"
        case dryRunImport = "dry_run_import"
        case idempotencyKeys = "idempotency_keys"
        case etagConflicts = "etag_conflicts"
        case changeFeed = "change_feed"
        case batchMutations = "batch_mutations"
        case tombstones
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(portableIds, forKey: .portableIds)
        try container.encode(numericIds, forKey: .numericIds)
        try container.encode(mobileSnapshot, forKey: .mobileSnapshot)
        try container.encode(dryRunImport, forKey: .dryRunImport)
        try container.encode(idempotencyKeys, forKey: .idempotencyKeys)
        try container.encode(etagConflicts, forKey: .etagConflicts)
        try container.encode(changeFeed, forKey: .changeFeed)
        try container.encode(batchMutations, forKey: .batchMutations)
        try container.encode(tombstones, forKey: .tombstones)
    }
}

