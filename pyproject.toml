[project]
name = "med-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "zensical>=0.0.24",
]

[dependency-groups]
dev = [
    "zensical>=0.0.24",
]
