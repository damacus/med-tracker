# Browser session API: test writer report

`rust/contract-tests/tests/web_session_api.rs` is a separate HTTP contract target. It uses the existing disposable fixture and a loopback-only cookie client. The root wired a dedicated `task api:web-session-acceptance` selection; this lane did not change the fixture, shared library, canonical dose selection, or production source.

The target compiles and covers standalone `/login` establishing a signed session with a rotated page CSRF token; cookie and bearer reads of the shared medication API; explicit invalid bearer precedence over a valid cookie; session CSRF and trusted-origin denials before a decimal dose write; a valid cookie write and replay with bearer-visible history; browser logout rejecting a copied old cookie; backing session-key deletion; and current membership suspension. The dose write uses the seeded low-stock tablet assignment, which the selected dose tests and browser flow do not mutate.

`task contract:fmt`, `task contract:clippy`, and `git diff --check` passed before the dedicated run. The runner invoked `task api:web-session-acceptance` against the frozen Rust API at baseline `2a8b4c1b`. All four selected cases failed in the common helper at `tests/web_session_api.rs:116`: standalone `/login` did not provide the expected CSRF token in HTML. No case reached its cookie, dose, logout, or invalidation assertions. This is a genuine first RED for the missing standalone login boundary, not evidence that the remaining behavior fails or passes. The disposable project `mtcontract-a7babd4cdd8a486f` and fixture were cleaned; the full Task log is `/Users/damacus/Library/Application Support/rtk/tee/1790347760_task_api_071818.log`.

The test file remains frozen for the root checkpoint. After that release, focused follow-up assertions should cover missing Origin/Referer, foreign household/person denial, and actual session expiry. Shared people, schedule and person-medication reads remain the next bounded test step.
