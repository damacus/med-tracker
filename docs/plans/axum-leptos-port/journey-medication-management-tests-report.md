# Medication management contract selection

The canonical `rust/contract-tests/run_medication_api_tests.fish` now appends the existing nine-case `medication_stock` target, raising the expected HTTP selection from 53 to 62 tests. Its `--no-fail-fast` and single-thread execution remain unchanged. No assertions, fixture records, or production source were changed for this test selection.

The nine existing Rails-derived cases cover medication creation, catalog/detail pagination and portable identity; conditional edit and conflict; invalid, foreign and view-only writes; decimal stock removal, replay and history; inventory adjustment; reorder/receipt transitions; delegated unlinked medication visibility; and request audit. Each successful mutation creates its own medication except the viewer reorder transition on the seeded managed medication; Cargo does not promise binary order from the target list, so that seeded transition needs an isolation check when the management route works. The audit-cap case creates 101 events and then checks the newest 100 within its own sequential test; no concurrently selected target shares the disposable fixture.

The existing cases previously passed their Rails baseline. The first Rust run selected 62 tests: 53 existing cases passed and nine management cases failed at medication collection POST with 405. This confirms the missing API route; those nine cannot exercise later stock/edit branches until creation works. `fish --no-execute`, `task contract:clippy` and `git diff --check` are the relevant local gates for this selection.

The separate `medication_management_security_api` target adds five focused tests: tracked dosage removal and aggregate stock, concurrent duplicate and limited-stock removals, replay after loss of manager role, concurrent conditional edit, and manager-only audit listing. Fresh medication records isolate each stock/edit case. The tracked case clears the single-dose amount after adding options, matching Rails tracked mode. Temporary membership role changes use a drop guard that restores the fixture role. The edit assertion counts Rails `api_update` versions. This target raises the canonical selection from 62 to 67 tests. `cargo fmt --check`, `fish --no-execute`, `task contract:clippy`, and `git diff --check` pass locally. The actual Rust RED is pending the runner's isolated acceptance. On the current pre-management API, creation returns 405, so the four stock/edit tests will stop at setup; this first RED cannot establish their deeper branches until creation is implemented. The audit authorization test reaches its own endpoint independently.

The `dose_mode_transition_api` target adds two independent HTTP cases and raises the canonical selection to 69. One converts a fresh option-mode medication with a direct source to a single dose and checks dosage deletion, the source link clearing, sync tombstones, one source and one medication change event, and one `api_update` version. The other keeps a schedule-linked option intact on 422 and checks that a `dose_amount: null` no-op remains allowed without sync effects. The tracked removal case also checks one dosage and one medication sync update event after the committed decrement. Fresh medications and direct disposable setup avoid changes to seeded fixture records. This target is compiled and formatted; its Rust outcome awaits isolated acceptance.

The `management_sync_events_api` target adds three focused cases and raises the selection to 72. They assert attributed `api_change_events` for medication create, edit, adjustment, reorder, ordinary removal, tracked dosage and parent stock removal, and a tracked direct take. The take event must carry the linked person's portable ID in metadata so a nonmanager's sync feed can select it. Identical replays and rejected writes must add no domain events; the ordinary case also checks fresh request audit on replay and rejection. Each case creates its own medication and uses unique request keys. Rust formatting, contract Clippy and diff checks pass; runtime RED and GREEN remain with the isolated runner. Source review found no static test blocker, while event counts alone do not prove the household cursor lock order.

The Rust management browser target also selects `medication-stale-edit.test.mjs` alongside the existing management journey file, making four Rust browser cases while the Rails target remains at three. The fake runner confirms Rust management selection and cleanup, including the stale edit file argument. Fish syntax and diff checks pass. The browser owner retains the test source and the isolated runner retains runtime verification.
