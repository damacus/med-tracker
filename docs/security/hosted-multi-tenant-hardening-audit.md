# Hosted Multi-Tenant Security Hardening Audit

Date: 2026-07-13

This audit is the launch gate for serving more than one independent household from one Rails application and one PostgreSQL database. It was reconciled against `main` for [#1614](https://github.com/damacus/med-tracker/issues/1614). A control is `GO` only when current repository evidence proves it; documentation of an intended control is not implementation evidence.

## Beta Decision

**Current status: NO-GO**

Decision recorded on 2026-07-13. Thirteen of twenty-two controls are `GO`. The hosted private beta remains closed on nine controls: owner and credential lifecycle, hosted invitation and support-session lifecycle, audit coverage for unimplemented offboarding actions, household export and purge, migration-time credential invalidation, and a production-like restore rehearsal.

## Decisions

- Tenant boundary: household.
- Deployment target: shared Rails app and shared PostgreSQL database.
- Platform admin: in-app operator capability, separate from household membership.
- Owner promotion: platform-admin-only during hosted beta.
- MFA/passkey: required before household admin pages are usable; fresh verification remains required for sensitive actions.
- API writes: in scope and must use membership-bound credentials, idempotency, version preconditions, and tenant-scoped authorization.
- Professional/carer access: household-only for this gate.
- Retention: configurable per deployer. Hosted beta defaults to export + purge unless a documented retention hold exists.

## Requirements Matrix

| Requirement | Current evidence | Gap / decision | Severity | Owner issue | Tests required | Beta status |
| --- | --- | --- | --- | --- | --- | --- |
| FR1 | [`SchemaInventory`](../../lib/schema_inventory.rb) classifies every primary application table, and [`schema_inventory_spec.rb`](../../spec/lib/schema_inventory_spec.rb) verifies the inventory. | Closed: every tenant-owned table is identified and carries `household_id`; global tables are explicitly classified. | High | Closed | [`schema_inventory_spec.rb`](../../spec/lib/schema_inventory_spec.rb). | GO |
| FR2 | [`20260624091000_enforce_strict_household_tenant_boundary.rb`](../../db/migrate/20260624091000_enforce_strict_household_tenant_boundary.rb) backfills derivable ownership, rejects ambiguous roots, and enforces non-null tenant keys represented in [`schema.rb`](../../db/schema.rb). | Closed: tenant-owned rows cannot remain unowned after migration. | Critical | Closed | [`schema_inventory_spec.rb`](../../spec/lib/schema_inventory_spec.rb). | GO |
| FR3 | Forced RLS and default-denial are implemented by the tenant-boundary migrations and verified against live PostgreSQL policies. | Closed: runtime access without tenant context is denied and cross-household reads are isolated. | Critical | Closed | [`household_row_level_security_spec.rb`](../../spec/models/household_row_level_security_spec.rb). | GO |
| FR4 | [`database.yml`](../../config/database.yml), [`docker-compose.yml`](../../docker-compose.yml), and [`init-roles.sql`](../../compose/init-roles.sql) separate the owner-capable migrator from `med_tracker_app`. | Closed: the runtime role is `NOBYPASSRLS`, cannot create in `public`, and does not inherit the owner role. | Critical | Closed | [`database_role_config_spec.rb`](../../spec/config/database_role_config_spec.rb), [`yaml_compose_spec.rb`](../../spec/config/yaml_compose_spec.rb), and [`household_row_level_security_spec.rb`](../../spec/models/household_row_level_security_spec.rb). | GO |
| FR5 | [`Api::V1::BaseController`](../../app/controllers/api/v1/base_controller.rb) binds credentials to an active household membership, [`TenantContext`](../../app/services/tenant_context.rb) sets database context, and support access is represented separately from membership. | Closed: web, API, and support-mode tenant context is derived from authorized account state rather than request-controlled identity. | Critical | Closed | [`write_resources_spec.rb`](../../spec/requests/api/v1/write_resources_spec.rb), [`base_controller_spec.rb`](../../spec/controllers/api/v1/base_controller_spec.rb), and [`support_access_sessions_spec.rb`](../../spec/requests/platform/support_access_sessions_spec.rb). | GO |
| FR6 | [`tenant_safety_spec.rb`](../../spec/architecture/tenant_safety_spec.rb) rejects unsafe tenant lookup patterns and verifies tenant context in jobs; API request specs exercise cross-household write rejection. | Closed: current web, API, attachment, and background-job boundaries have automated tenant-safety coverage. | High | Closed | [`tenant_safety_spec.rb`](../../spec/architecture/tenant_safety_spec.rb), [`write_resources_spec.rb`](../../spec/requests/api/v1/write_resources_spec.rb), and [`sync_safety_spec.rb`](../../spec/requests/api/v1/sync_safety_spec.rb). | GO |
| FR7 | [`Admin::MembershipRoleUpdater`](../../app/services/admin/membership_role_updater.rb) isolates non-owner role changes and [`user_mutation_boundary_spec.rb`](../../spec/requests/admin/user_mutation_boundary_spec.rb) rejects generic role/grant mutation and owner promotion. | Owner promotion has no platform-admin flow, and web access changes do not consistently invalidate credentials. Launch impact: existing access can outlive a role or grant change, so hosted onboarding is blocked. | Critical | [#1618](https://github.com/damacus/med-tracker/issues/1618) | Owner promotion, role/grant invalidation, fresh-MFA, owner-governance, and audit specs required by #1618. | NO-GO |
| FR8 | [`Platform::SupportAccessSessionsController`](../../app/controllers/platform/support_access_sessions_controller.rb), [`SupportAccessSession`](../../app/models/support_access_session.rb), and the [support access runbook](../operations/hosted-private-beta-runbook.md#support-access) require platform admin, fresh MFA, a reason, expiry, and redacted start/end audit events. | Natural expiry removes authorization but has no idempotent operational audit event. Launch impact: operator access expiry cannot be demonstrated end-to-end, so hosted support access is blocked. | Critical | [#1619](https://github.com/damacus/med-tracker/issues/1619) | [`support_access_sessions_spec.rb`](../../spec/requests/platform/support_access_sessions_spec.rb), [`support_access_session_spec.rb`](../../spec/models/support_access_session_spec.rb), plus natural-expiry processing required by #1619. | NO-GO |
| FR9 | [`invitations_spec.rb`](../../spec/requests/invitations_spec.rb) proves pinned email and rejected expired/reused tokens; [`household_invitation_spec.rb`](../../spec/models/household_invitation_spec.rb) proves raw tokens are excluded from audit versions. | Acceptance under the deployed `med_tracker_app` role and cross-household invitation visibility are not explicitly proven. Launch impact: hosted invitation onboarding remains blocked. | High | [#1619](https://github.com/damacus/med-tracker/issues/1619) | Current invitation request/model specs plus runtime-role and cross-household cases required by #1619. | NO-GO |
| FR10 | [`HostedPrivilegedActionMfa`](../../app/controllers/concerns/hosted_privileged_action_mfa.rb) protects hosted admin pages; [`Api::FreshPrivilegedAction`](../../app/services/api/fresh_privileged_action.rb) and platform controller gates require fresh proof for sensitive writes. | Closed: local MFA/passkey and upstream OIDC MFA evidence are enforced at the current web, platform, and API privileged boundaries. | Critical | Closed | [`admin_mfa_gate_spec.rb`](../../spec/requests/admin_mfa_gate_spec.rb), [`fresh_privileged_action_spec.rb`](../../spec/services/api/fresh_privileged_action_spec.rb), [`settings_spec.rb`](../../spec/requests/platform/settings_spec.rb), and [`admin_api_spec.rb`](../../spec/requests/api/v1/admin_api_spec.rb). | GO |
| FR11 | API sessions and app tokens are membership-bound and reject stale permission versions; API writes implement idempotency, portable IDs, ETags, stale-write responses, and cross-household rejection. | [`Admin::MembershipRoleUpdater`](../../app/services/admin/membership_role_updater.rb) and some grant/status paths do not advance `permissions_version`. Launch impact: previously issued credentials can retain obsolete access, so hosted API use is blocked. | Critical | [#1618](https://github.com/damacus/med-tracker/issues/1618) | [`api_session_household_spec.rb`](../../spec/models/api_session_household_spec.rb), [`api_app_token_spec.rb`](../../spec/models/api_app_token_spec.rb), [`sync_safety_spec.rb`](../../spec/requests/api/v1/sync_safety_spec.rb), plus every access-change path required by #1618. | NO-GO |
| FR12 | Notification jobs reload household/person state in tenant context, enforce recipient preferences/access, and use private generic content; push service specs verify sensitive payload and log redaction. | Closed: notification privacy and recipient authorization are covered for current delivery paths. Durable provider delivery remains separately tracked in [#1611](https://github.com/damacus/med-tracker/issues/1611) and is not this security control. | Critical | Closed | [`low_stock_notification_job_spec.rb`](../../spec/jobs/low_stock_notification_job_spec.rb), [`missed_dose_notification_job_spec.rb`](../../spec/jobs/missed_dose_notification_job_spec.rb), [`medication_reminder_job_spec.rb`](../../spec/jobs/medication_reminder_job_spec.rb), and [`push_notification_service_spec.rb`](../../spec/services/push_notification_service_spec.rb). | GO |
| FR13 | Tenant-owned notification and reminder jobs accept a household identifier, enter [`TenantContext`](../../app/services/tenant_context.rb), and reload records inside the context; architecture coverage inventories those boundaries. | Closed: current tenant-owned jobs carry tenant context and do not serialize sensitive model payloads as job arguments. | High | Closed | [`tenant_safety_spec.rb`](../../spec/architecture/tenant_safety_spec.rb) and the notification job specs listed for FR12. | GO |
| FR14 | The [`Audit::Event`](../../app/services/audit/event.rb) contract records tenant, actor, request, authorization, and redacted metadata; chained ledger entries, signed checkpoints, separated exporter/verifier roles, WORM delivery, and verification have focused coverage. | Export, retention-hold, offboarding, and purge events cannot be complete until those workflows exist. Launch impact: the hosted data-lifecycle audit trail is incomplete, so the beta is blocked. | High | [#1620](https://github.com/damacus/med-tracker/issues/1620) | Existing [`audit`](../../spec/services/audit) service specs plus export/offboarding audit cases required by #1620. Broader forensic follow-up remains in [#509](https://github.com/damacus/med-tracker/issues/509). | NO-GO |
| FR15 | Portable API export, scoped reports, credential revocation primitives, and the [procedural runbook](../operations/hosted-private-beta-runbook.md#export-and-purge) exist. | No complete household export, configurable retention-hold model, idempotent offboarding coordinator, or complete purge exists. Launch impact: a hosted household cannot be safely exited or deleted, so the beta is blocked. | Critical | [#1620](https://github.com/damacus/med-tracker/issues/1620) | Export authorization/completeness, retention hold, revocation, retry, isolation, and purge specs required by #1620. | NO-GO |
| FR16 | [`HealthHistoryReportsController`](../../app/controllers/health_history_reports_controller.rb) uses `policy_scope(Person)`, generates the PDF in memory, and sends it with `Cache-Control: no-store`. | Report generation/download is not audited and the PDF is not a complete household export. Launch impact: export evidence is incomplete, so the hosted data-lifecycle gate remains blocked. | High | [#1620](https://github.com/damacus/med-tracker/issues/1620) | [`reports_spec.rb`](../../spec/requests/reports_spec.rb) plus audited export lifecycle and cross-household completeness cases required by #1620. | NO-GO |
| FR17 | Active Storage attachments carry non-null `household_id`, forced RLS covers attachment rows, direct uploads are disabled by default, and avatars are served through an application-owned authorized route. | Closed: current tenant-owned attachment records and delivery routes are covered by tenant isolation checks. | High | Closed | [`household_row_level_security_spec.rb`](../../spec/models/household_row_level_security_spec.rb), [`tenant_safety_spec.rb`](../../spec/architecture/tenant_safety_spec.rb), and profile/avatar request specs. | GO |
| FR18 | [`AllowlistedSpanExporter`](../../lib/otel/allowlisted_span_exporter.rb) restricts telemetry attributes; audit context/event and notification specs reject sensitive metadata and content in logs. | Closed: current logs, spans, audit metadata, job payloads, and push paths have explicit redaction coverage. New export/offboarding paths must satisfy the same contract before their owning controls can close. | High | Closed | [`allowlisted_span_exporter_spec.rb`](../../spec/lib/otel/allowlisted_span_exporter_spec.rb), audit context/event specs, and the FR12 notification specs. | GO |
| NFR1 | Every FR/NFR row names current automated evidence or the exact missing tests owned by a linked issue; this matrix is itself parsed by [`schema_inventory_hosted_multitenant_hardening_documentation_spec.rb`](../../spec/lib/schema_inventory_hosted_multitenant_hardening_documentation_spec.rb). | Closed: the test inventory is explicit and unresolved test work cannot be marked `GO` without updating this executable gate. | High | Closed | This documentation spec plus every focused path named above. | GO |
| NFR2 | The strict tenant migration is idempotent for assigned rows, fails ambiguous ownership with diagnostics, and the [pre-0.5 upgrade runbook](../pre-0-5-database-upgrade.md) documents owner/runtime roles and preflight. | Closed: migration/backfill strategy and runtime-role compatibility are implemented and documented. | Critical | Closed | [`schema_inventory_spec.rb`](../../spec/lib/schema_inventory_spec.rb), [`task_pre_0_5_database_upgrade_preflight_spec.rb`](../../spec/tasks/rake/task_pre_0_5_database_upgrade_preflight_spec.rb), and database role specs. | GO |
| NFR3 | The migration preserves an unambiguous existing self-hosted household, and current credentials carry membership permission versions. | The migration-time decision for existing browser/API/device/export credentials is not implemented across every access-change path. Launch impact: old credentials can survive hosted conversion with stale authority, so migration approval is blocked. | High | [#1618](https://github.com/damacus/med-tracker/issues/1618) | Backward-compatibility migration and credential invalidation cases required by #1618. | NO-GO |
| NFR4 | The [hosted private beta runbook](../operations/hosted-private-beta-runbook.md) defines `task hosted-restore:rehearse`, owner/runtime/verifier role separation, runtime-image binding, transaction-scoped RLS checks, exact restored/WORM head inclusion, durable-root containment, sanitized JSON and Markdown evidence, quarterly cadence, and invalidation triggers. | Safe executable automation exists, but no current production-like database and attachment restore evidence is linked. Launch impact: operators cannot yet demonstrate recovery for the intended deployment, so the beta is blocked. | Critical | [#1621](https://github.com/damacus/med-tracker/issues/1621) | [`runtime_verifier_spec.rb`](../../spec/services/hosted_restore/runtime_verifier_spec.rb), [`audit_verifier_spec.rb`](../../spec/services/hosted_restore/audit_verifier_spec.rb), [`rehearsal_spec.rb`](../../spec/lib/hosted_restore/rehearsal_spec.rb), plus an independently inspected real rehearsal evidence link required by #1621. | NO-GO |

## Open Blocker Register

| Issue | Owner | Severity | Controls | Remediation | Launch impact |
| --- | --- | --- | --- | --- | --- |
| [#1618](https://github.com/damacus/med-tracker/issues/1618) | Backend/security | Critical | FR7, FR11, NFR3 | Centralize access changes, advance permission versions, invalidate stale credentials, and add fresh-MFA platform owner promotion. | Hosted onboarding, administration, API access, and migration remain blocked. |
| [#1619](https://github.com/damacus/med-tracker/issues/1619) | Backend/security | Critical | FR8, FR9 | Prove invitation acceptance under the runtime role and add idempotent audited support-session expiry. | Hosted invitation onboarding and operator support remain blocked. |
| [#1620](https://github.com/damacus/med-tracker/issues/1620) | Backend/security | Critical | FR14, FR15, FR16, NFR4 | Implement complete export, retention holds, audited offboarding, credential/device revocation, and tenant-safe purge. | Household exit, deletion, and data-lifecycle auditability remain blocked. |
| [#1621](https://github.com/damacus/med-tracker/issues/1621) | Operations/security | Critical | NFR4 | Perform and record a production-like database, attachment, RLS, and WORM restore rehearsal. | Disaster-recovery approval remains blocked. |

## Related Non-Gate Work

- [#1608](https://github.com/damacus/med-tracker/issues/1608) made mobile sync authoritative and conflict-safe in merged [PR #1616](https://github.com/damacus/med-tracker/pull/1616). It closes a mobile launch dependency without changing a hosted security row here.
- [#1609](https://github.com/damacus/med-tracker/issues/1609) completes native MFA and multi-household authentication; it is a mobile launch dependency rather than this hosted web beta gate.
- [#1611](https://github.com/damacus/med-tracker/issues/1611) makes native notification delivery durable; current notification privacy is `GO`, while durable provider delivery remains a mobile launch dependency.
- [#509](https://github.com/damacus/med-tracker/issues/509) tracks broader forensic audit hardening. Current chaining, signed checkpoint, WORM delivery, verifier separation, and redaction evidence is reflected above; the unimplemented hosted data-lifecycle events remain gated by #1620.

### Hosted credential compatibility

Credentials created before the hosted membership-version binding are not migrated into an implicitly trusted state. API sessions, app tokens, and OAuth grants must carry both a current household membership binding and the exact current `permissions_version`; a missing binding or version fails closed and requires the account to authenticate and issue a new credential. Every effective membership role, status, person reassignment, or person-access grant change advances the affected membership version atomically, so credentials issued before that change are rejected on their next web-adjacent API or direct API request.

## Go/No-Go Checklist

- [x] Runtime app DB role is `NOBYPASSRLS`, is not an owner, and cannot create in `public`.
- [x] Tenant-owned tables enforce non-null household ownership.
- [x] RLS default-denies without tenant context and rejects cross-household access.
- [x] Current web/API tenant access derives from active membership or explicit support mode.
- [x] Generic admin user update cannot mutate roles or person grants.
- [x] Hosted admin pages and privileged mutations require MFA evidence.
- [x] Current notifications are private by default and recipient-aware.
- [x] Attachments, logs, audit metadata, and telemetry have tenant/redaction coverage.
- [ ] Platform-admin owner promotion and access-change credential invalidation are complete: #1618.
- [ ] Invitation acceptance is proven under runtime RLS and support expiry is operationally audited: #1619.
- [ ] Household export, retention, offboarding, and purge are implemented and audited: #1620.
- [ ] A production-like backup restore rehearsal has passed and its evidence is linked: #1621.

## Review Process

- Reconcile this matrix whenever a pull request changes authentication, authorization, tenancy, invitations, support access, API writes, notifications, exports, retention, deletion, attachments, audit, telemetry, migrations, deployment roles, or restore behavior.
- The owner of a linked blocker updates its row with code, test, configuration, runbook, and deployment evidence before changing `NO-GO` to `GO`.
- A status change must update the blocker register and checklist in the same pull request. Closing an issue alone is not evidence.
- Before each hosted release, run this documentation spec, the focused tests linked by the affected rows, `task rubocop`, `task test`, and `task brakeman`.
- Record the hosted decision date after every reconciliation. The beta remains closed while any matrix row is `NO-GO`.
