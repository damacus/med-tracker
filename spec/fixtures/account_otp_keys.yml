# frozen_string_literal: true

# OTP keys for accounts that require 2FA (administrators, doctors, nurses)
# The key is a base32-encoded secret used for TOTP generation
# All test accounts use the same secret for simplicity: JBSWY3DPEHPK3PXP

damacus:
  id: <%= ActiveRecord::FixtureSet.identify(:damacus) %>
  key: JBSWY3DPEHPK3PXP
  num_failures: 0
  last_use: <%= Time.current %>
