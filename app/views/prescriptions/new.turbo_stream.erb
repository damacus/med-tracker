<%= turbo_stream.update "modal" do %>
  <%= render "shared/modal", title: "New Prescription for #{@person.name}" do %>
    <%= render Components::Prescriptions::Form.new(
      prescription: @prescription,
      person: @person,
      medicines: @medicines
    ) %>
  <% end %>
<% end %>
