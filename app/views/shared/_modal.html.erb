<dialog id="modal" class="modal" data-controller="modal">
  <div class="modal__container">
    <div class="modal__header">
      <h2 class="modal__title"><%= title %></h2>
      <button class="modal__close" type="button" onclick="this.closest('dialog').close()">×</button>
    </div>

    <div class="modal__content">
      <%= content_for?(:modal_content) ? yield(:modal_content) : (local_assigns[:content] || yield) %>
    </div>
  </div>
</dialog>
